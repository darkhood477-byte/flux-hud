#!/bin/bash
sed -i -z 's/appRepository.addClipboardItem(contentToSave)\n                                        }/appRepository.addClipboardItem(contentToSave)\n                                        }\n                                        try {\n                                            val vibrator = this@OverlayService.getSystemService(android.content.Context.VIBRATOR_SERVICE) as android.os.Vibrator\n                                            if (android.os.Build.VERSION.SDK_INT >= android.os.Build.VERSION_CODES.Q) {\n                                                vibrator.vibrate(android.os.VibrationEffect.createPredefined(android.os.VibrationEffect.EFFECT_CLICK))\n                                            } else {\n                                                vibrator.vibrate(50)\n                                            }\n                                        } catch (e: Exception) {}/g' /app/applet/app/src/main/java/com/example/overlay/OverlayService.kt
